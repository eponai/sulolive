(ns re-natal.support)

;; Contains stubs for re-natal.support
;; which is used for our react-native code.
;; Only used for web compilation.

(defonce root-nodes nil)

(defn root-node!
  [id]
  ;; stub
  )

(defn root-render
  [el id]
  ;; stub
  )

(defn root-unmount
  [id]
  ;; stub
  )
